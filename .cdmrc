# Tabs opened at boot
tabs: ['client/src/getWeb3.js', 'truffle-config.js', 'client/src/index.js']
# Command run on terminal one
terminal-one: cd client && yarn install && yarn dev
terminal-two: truffle develop
